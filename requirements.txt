# Core — PyTorch avec support CUDA
# Installer PyTorch séparément selon la version CUDA :
# CUDA 12.1 : pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu121
# CUDA 12.4 : pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu124
torch>=2.2.0

# HuggingFace ecosystem
transformers>=4.45.0
datasets>=2.21.0
tokenizers>=0.19.0
huggingface_hub>=0.24.0
safetensors>=0.4.4
accelerate>=0.34.0

# PEFT — LoRA et adapteurs
peft>=0.13.0

# TRL — SFTTrainer et entraînement supervisé
trl>=0.12.0

# QLoRA — quantification 4-bit NF4
# Version spécifique selon la version CUDA et le GPU
# Pour CUDA 12.x sur Linux : pip install bitsandbytes>=0.43.0
# Pour Windows : pip install bitsandbytes>=0.43.0 (support experimental)
bitsandbytes>=0.43.0

# Utilitaires
numpy>=1.26.0
scipy>=1.13.0

# Optionnel — monitoring de l'entraînement
# wandb>=0.17.0
# tensorboard>=2.17.0
